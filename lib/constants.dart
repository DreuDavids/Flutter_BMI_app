import 'package:flutter/material.dart';

//Text styles
const defaultLabelTextStyle = TextStyle(
  letterSpacing: 1.5,
  fontSize: 18,
  color: Color(0xFF8D8E98),
);
const defaultNumberTextStyle =
    TextStyle(fontSize: 50, fontWeight: FontWeight.w900);

//APP BAR Title style
const defaultAppTitleTextStyle = TextStyle(letterSpacing: 3);

//Bottom Container
const defaultBottomContainerHeight = 80.0;
const defaultBottomContainerColour = Colors.orange;

//Card
const defaultActiveCardColour = Color(0XFF1D1E33);
const defaultInactiveCardColour = Color(0xFF111328);
const Color maleCardColour = defaultInactiveCardColour;
const Color femaleCardColour = defaultInactiveCardColour;

//Icon
const defaultActiveIconColour = Colors.orange;
const defaultInactiveIconColour = Colors.white;

//Slider
const defaultActiveSliderColour = Colors.white;
const defaultInactiveSliderColour = Color(0xFF8D8E98);
const defaultSliderThumbColour = Colors.orange;
const defaultSliderOverlayColour = Colors.white;
const defaultSliderValueIndicatorColour = Colors.orange;
